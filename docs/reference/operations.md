# Aervox｜思隅 运行、值班与演练手册

> 文档编号：AVX-OPS-001（合并 RUNBOOK + ONCALL + DRILL_TEMPLATE）  
> 版本：v0.1（评审候选）  
> 更新日期：2026-08-26  
> 状态：Review Candidate  
> Owner/值班：待指定  
> 关联：[架构设计](ARCHITECTURE.md) · [数据与隐私](DATA_PRIVACY.md) · [威胁模型](THREAT_MODEL.md)

本文档合并原《运行与恢复手册》《值班与升级联系矩阵》《季度恢复演练证据模板》三份文档，覆盖生产故障止损/恢复/验证（§1-§10）、值班角色与升级（§11）、季度演练证据模板（§12）。

## 1. 服务与 SLO

| 服务 | 关键 SLI/SLO | 主要告警 |
|---|---|---|
| API/SSE | 可用性 99.5%/月；非 AI 读/写 P95 300/500 ms | 5xx、延迟、连接中断、限流异常 |
| AI Gateway | TTFT P95 ≤ 8 s；教学/安全质量门槛 | 超时、限流、成本、模型/Prompt 回归 |
| Worker/Queue | 日记首版 95% ≤ 15m；复习/通知 P95 ≤ 5m | queue lag、DLQ、重复结果、worker crash |
| Data/Deletion | 在线删除 ≤ 24h；删除后零召回 | DeletionTarget 失败/积压、索引残留 |
| SQLite/S3 | 数据完整、备份可恢复 | 容量、复制延迟、PITR、对象错误 |

### 1.1 快速入口

- 值班与升级联系矩阵：[§11 值班与升级](#11-值班与升级联系矩阵)
- 季度演练证据模板：[§12 演练证据模板](#12-季度恢复演练证据模板)

## 2. 通用事件流程

1. 确认告警真实性、影响范围、开始时间、版本和最近变更。
2. 指定 Incident Commander、通信人和记录人；高严重度立即冻结发布。
3. 优先止损：Feature Flag、熔断、只读、关闭非核心任务、撤销插件/供应商。
4. 保留最小审计证据，不把完整用户内容复制到工单或聊天。
5. 恢复后验证业务事实、来源链、删除状态和监控；24/72 小时内完成复盘和 `CR/RISK`。

严重度：SEV-0 数据大规模泄露/不可逆损坏/安全危机响应系统性失效；SEV-1 核心闭环大面积不可用或删除链失效；SEV-2 局部功能/供应商降级；SEV-3 低影响缺陷。

## 3. 模型供应商故障

- 触发：超时/5xx/限流超过路由阈值、质量门槛回归或供应商安全事件。
- 动作：熔断受影响模型；切换已通过同版本 EvalSet 的备用模型；无备用时保留输入并提供重试/历史只读。
- 不得：跳过输入/输出安全分类、使用未评估模型、把失败输出写入记忆/日记/掌握度。
- 验证：ModelRun、路由比例、TTFT、事实/安全抽检、成本；恢复需渐进放量。

## 4. Redis/队列丢失或积压

- 暂停非核心生产者和重复重放；保留 API 核心写入及 SQLite Outbox。
- 恢复 Redis 后按 Outbox/ScheduledJob 水位线重建队列；使用幂等键，检查每个 `(workspaceId, subjectUserId, localDate)` 标签最多一个自动日记身份、复习活动项唯一和删除任务状态。
- DLQ 重放前检查权限/删除状态/版本，过期或撤销任务标记取消，不再次调用供应商。
- 验证 queue lag、重复业务结果、Outbox 未消费数和 Worker trace。

## 5. SQLite 故障与备份恢复

- 进入只读/维护状态，停止 Worker 写入和外部同步；保护当前 WAL/备份证据。
- 按批准 RPO 选择恢复点，在隔离环境恢复并运行完整性/迁移检查。
- `RecoveryControlLedger` 是独立故障域的 deny 控制事实源。先在全局 deny/维护状态校验签名、序列连续性、最老备份覆盖范围和业务应用水位；水位缺口、账本不可用或 reconciliation 未完成时保持 fail closed，不开放流量。按 `sequence` 幂等重放删除、撤销的 ConsentGrant、插件权限和外部授权，再重建 Redis、FTS/pgvector 和派生投影。
- 冒烟验证账户/工作区隔离、`actorId` 与 `subjectUserId` 分离、学习记录、日记唯一性、记忆来源、导出和删除后零召回。
- 不达 RPO/RTO 时升级 SEV-1 并通知批准角色。

## 6. 对象存储/附件事件

- 立即撤销受影响签名 URL，暂停下载/解析；检查对象版本、扫描状态和工作区范围。
- 从可信副本恢复时校验 checksum，重新运行病毒/OCR/来源处理；已删除对象不能恢复为可见状态。
- 对外泄风险按 SEV-0/1 处置，并触发安全/隐私通知流程。

## 7. 删除传播失败

- DeletionRequest 一旦接受，来源保持 deny，无论后台状态如何。
- 定位失败的 DeletionTarget（DB、Redis、FTS/vector、S3、Diary/Memory、供应商、备份）；重试不得恢复访问。
- 超过 SLA 触发 SEV-1、用户可见状态和安全/隐私 Owner；外部供应商按合同升级。
- 完成后运行零召回、对象清单、树投影 diff 和导出缺失状态检查，证据回填请求。

## 8. 日记/记忆流水线异常

- 日记重复：以 `(workspaceId,subjectUserId,localDate,autoGenerated)` 唯一身份为准，表示 `localDate` 标签只有一个自动日记身份；保留合法版本并标记异常运行，不删除用户编辑。
- 日记迟到：首版不等 grace；`bufferClosedAt` 前到达且 `occurredAt` 在本 Cycle 滚动窗口内的延迟来源只产生补写候选，关闭后到达的本窗口来源不自动改写或产生候选；只有 `occurredAt > cutoffAt` 才进入下一周期，早于或等于窗口下界的历史迟到来源只能手动关联或忽略。停用/重新启用时以新 `initialWindowStart` 开始，不回填停用期间素材。
- 记忆过度压缩/虚假事实：关闭高层晋升和树召回，保留临时/短期来源，回滚算法/Prompt，重建受影响投影。
- 来源不可追溯：立即隐藏相应日记段落/记忆节点并阻断发布/召回。

## 9. 发布与回滚

- 发布前：备份/迁移验证、Feature Flag、容量、Eval、OpenAPI diff、许可证/安全扫描、值班和回滚命令完成。
- 灰度：内部→1%→10%→50%→100%，每级检查错误预算、AI 质量、安全、成本和删除积压。
- 应用回滚不能回滚数据库到丢失新数据；使用 expand/contract 和兼容开关。不可逆迁移为发布阻断。

## 10. 演练与证据

每季度演练 SQLite 备份恢复/Litestream、RecoveryControlLedger 账本不可用/缺口/重复/乱序及 reconciler、Redis 丢失重建、S3 恢复、模型中断、删除传播、插件 kill switch 和日记跨 DST。记录日期、环境、版本、RPO/RTO、账本水位、fail-closed 结果、偏差、Owner 和改进项；没有演练证据不能通过 G5。执行步骤见 [工程与发布流程 §3](../how-to/engineering-process.md#3-执行季度恢复演练)，证据记录用 [§12 演练证据模板](#12-季度恢复演练证据模板)。

## 11. 值班与升级联系矩阵

### 11.1 严重度分级（与 §2 一致）

| 级别 | 定义 | 典型示例 |
|---|---|---|
| `SEV-0` | 数据大规模泄露、不可逆损坏或安全危机响应系统性失效 | 删除后数据复活、凭据泄露、危机响应失效 |
| `SEV-1` | 核心闭环大面积不可用或删除链失效 | 核心 API 大面积不可用、删除传播超 SLA |
| `SEV-2` | 局部功能或供应商降级 | 单模型降级、队列积压、局部功能异常 |
| `SEV-3` | 低影响缺陷 | 可计划修复的缺陷 |

### 11.2 值班角色与联系（人名待定）

| 角色 | 职责范围 | 联系方式 | 替代人 |
|---|---|---|---|
| 技术负责人 | 架构、容量、迁移、可靠性 | 待填 | 待填 |
| 运维/平台负责人 | 部署、监控、恢复、演练 | 待填 | 待填 |
| 安全与隐私负责人 | 安全事件、数据生命周期、合规 | 待填 | 待填 |
| AI 质量负责人 | 模型/Prompt 质量与回滚 | 待填 | 待填 |
| QA 负责人 | 测试证据、发布门禁 | 待填 | 待填 |
| 产品负责人 | 范围、变更批准、对外沟通 | 待填 | 待填 |

### 11.3 升级矩阵

| SEV | 首要联系人 | 响应时限 | 未响应升级 | 通报范围 |
|---|---|---|---|---|
| `SEV-0` | 安全与隐私负责人 + 技术负责人 | 15 分钟 | 技术负责人 → 产品负责人 | 全组 + 管理层 |
| `SEV-1` | 运维/平台负责人 | 30 分钟 | 技术负责人 | 值班 + 相关 Owner |
| `SEV-2` | 相关模块 Owner | 4 小时 | 技术负责人 | 值班 |
| `SEV-3` | 相关模块 Owner | 下一个工作日 | 无 | 无 |

> 响应时限为草案默认值，正式生效需技术/运维负责人批准。

### 11.4 事件流程与交接

- 每次事件指定 Incident Commander、通信人与记录人；高严重度立即冻结发布（见 [§2 通用事件流程](#2-通用事件流程)）。
- 交接单至少包含：事件时间线、已做止损、剩余风险、待办与负责人。
- 涉及用户数据的工单不得复制完整正文（见[数据与隐私](DATA_PRIVACY.md)）。
- 演练期间的联系与审批见 [§12 演练证据模板](#12-季度恢复演练证据模板)。

## 12. 季度恢复演练证据模板

每个季度用一个本模板实例记录全部演练项证据；无演练证据不能通过 G5（见 [§10 演练与证据](#10-演练与证据)）。执行步骤见 [工程与发布流程 §3](../how-to/engineering-process.md#3-执行季度恢复演练)。

### 12.1 演练基本信息

| 字段 | 值 |
|---|---|
| 演练日期 / 环境 / 版本 | |
| 演练负责人 / 审批人 | |
| 目标 RPO / RTO | |
| 涉及的供应商/依赖 | |
| 演练前值班确认 | |

### 12.2 演练项与证据

| # | 演练项 | 通过标准 | 结果 PASS/FAIL | 账本水位/序列 | fail-closed 结果 | 偏差 | 改进项 | Owner |
|---|---|---|---|---|---|---|---|---|
| 1 | SQLite/Litestream 备份恢复与完整性校验 | 恢复演练达到已批准 RPO/RTO | | | | | | |
| 2 | `RecoveryControlLedger` 不可用/缺口/重复/乱序 + reconciler | 按 sequence 幂等重放；账本不可用/缺口时 fail closed | | | | | | |
| 3 | Redis 丢失重建 | 从 Outbox/ScheduledJob 重建，无重复业务结果 | | | | | | |
| 4 | S3/对象恢复与 checksum | 校验通过，不恢复已删除对象 | | | | | | |
| 5 | 模型供应商中断与备用切换 | 切换已评估备用、保留输入；不跳过安全门 | | | | | | |
| 6 | 删除传播与零召回 | 24 小时内清除 Aervox 在线副本/索引/缓存，零召回 | | | | | | |
| 7 | 插件/外部集成 kill switch | 越权即停、保留审计与撤权证据 | | | | | | |
| 8 | 日记跨 DST 与多日停机补跑 | 窗口无重叠/空洞，按截止点顺序恢复 | | | | | | |

### 12.3 复盘与关闭

- 偏差项建立的 `CR-*` / `RISK-*` 编号：
- 下季度前需关闭的改进项：
- 演练整体结论与批准人：
