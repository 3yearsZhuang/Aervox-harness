name: Docs

on:
  pull_request:
    paths:
      - "docs/**"
      - "README.md"
      - ".markdownlint-cli2.jsonc"
      - ".github/workflows/docs.yml"
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "README.md"
      - ".markdownlint-cli2.jsonc"

permissions:
  contents: read

jobs:
  lint:
    name: Markdown lint & link check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Markdown lint
        uses: DavidAnson/markdownlint-cli2-action@v19
        with:
          globs: |
            docs/**/*.md
            README.md
          config: .markdownlint-cli2.jsonc

      - name: Link check (docs + README, exclude reference/ and demos/)
        uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --accept 200,429,403 README.md docs
