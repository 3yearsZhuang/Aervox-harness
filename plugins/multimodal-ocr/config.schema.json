{
  "apiVersion": "aervox.dev/v1",
  "kind": "PluginConfigSchema",
  "schemaVersion": 1,
  "fields": [
    {
      "key": "ocrEngine",
      "type": "select",
      "label": "OCR 识别引擎",
      "description": "选择用于图片字符与公式提取的后台引擎",
      "default": "mock",
      "options": [
        {
          "value": "mock",
          "label": "本地轻量内置 OCR (快速测试与离线保底)"
        },
        {
          "value": "tesseract",
          "label": "Tesseract 本地光学字符识别"
        },
        {
          "value": "mathpix",
          "label": "Mathpix 复杂数学公式与图表解析引擎"
        }
      ]
    },
    {
      "key": "confidenceThreshold",
      "type": "number",
      "label": "置信度最低过滤阈值",
      "description": "低于此置信度的文字将被标注为可疑并在提示词中要求用户确认（0.1~1.0）",
      "default": 0.8,
      "validation": {
        "min": 0.1,
        "max": 1
      }
    },
    {
      "key": "autoExtractLatex",
      "type": "boolean",
      "label": "自动提取 LaTeX 数学公式",
      "description": "识别到行内/行间数学公式时自动用 $ 或 $$ 包裹输出",
      "default": true
    }
  ]
}
